<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MangaVerse" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/manga_primary</item>
        <item name="colorPrimaryDark">@color/manga_primary_variant</item>
        <item name="colorAccent">@color/manga_secondary</item>
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/manga_background</item>
        
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/manga_primary_variant</item>
    </style>
</resources>